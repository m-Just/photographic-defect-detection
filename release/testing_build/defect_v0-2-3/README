Testing specification for model defect_v0-2-3

Input format:
  8-bit RGB of size 224x224 (resized via area interpolation, e.g. cv2.INTER_AREA),
  then normalize pixel values to [0, 1].

Output format:
  Three floats representing the predicted scores for:
    1. Bad Exposure
    2. Bad Saturation
    3. Undesired Blur
